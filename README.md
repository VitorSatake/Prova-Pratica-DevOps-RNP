# Prova-Pratica-DevOps-RNP
Repositório destinado para a prova prática do processo de DevOps RNP.

Para utlização é necessário ter instalado o Docker e o Docker Compose.

# Comando para executar:

Na raiz do projeto, execute: docker-compose up --build -d