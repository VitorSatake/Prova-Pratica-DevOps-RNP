# Exemplo de variáveis de ambiente para docker-compose
# Copie este arquivo para `.env` e ajuste valores sensíveis antes de rodar o stack.

# Postgres
POSTGRES_USER=monitor
POSTGRES_PASSWORD=monitor123
POSTGRES_DB=metrics

# Banco usado pelos agentes (poderá ser o mesmo Postgres)
DB_HOST=postgres
DB_USER=monitor
DB_PASSWORD=monitor123
DB_NAME=metrics

# Grafana
GF_SECURITY_ADMIN_PASSWORD=admin
GF_AUTH_ANONYMOUS_ENABLED=true
GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
